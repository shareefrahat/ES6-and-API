const stuInfo = JSON.stringify({ name: "James", roll: 3 });
console.log(stuInfo.name);

const info = { data: [{ language: "Javascript", library: "React" }] };

const output = info.data[0].library;
console.log(output);
